require 'minitest/autorun'
require_relative '../../lib/commands'

describe CommandsManager do
  describe "when adding a custom command" do
    it "updates the database" do
    end

    it "adds the command to the bot" do
      skip "not yet"
    end

    it "responds to chat with a success" do
      skip "not yet"
    end
  end
end
